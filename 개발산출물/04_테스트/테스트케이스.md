# 테스트케이스

**작성일**: 2026-07-08 | **버전**: 1.0

---

## TC-DASH: 통합 대시보드

| ID | 시나리오 | 사전조건 | 절차 | 예상결과 |
|---|---|---|---|---|
| TC-DASH-01 | 경영진단 요약 조회 | 로그인 완료 | `GET /api/erp-sync/dashboard/executive-summary/?period_value=오늘월` | 200, `total_sales>0` |
| TC-DASH-02 | 영업 대시보드 조회 | 매출 데이터 존재 | `GET .../sales/?date=오늘` | 200, `monthly_sales`가 날짜별로 다름 |
| TC-DASH-03 | 생산 대시보드 조회 | 생산 데이터 존재 | `GET .../production/?date=오늘` | 200, `achievement_rate` 반환 |
| TC-DASH-04 | 품질 대시보드 조회 | 검사 데이터 존재 | `GET .../quality/?date=오늘` | 200, `yield_by_process[]` 비어있지 않음 |
| TC-DASH-05 | 재고 대시보드 조회 | 재고 데이터 존재 | `GET .../inventory/?asof_date=오늘` | 200, `slow_moving_details[]` 5건 이하 |
| TC-DASH-06 | 통합 대시보드 화면 렌더링 | 백엔드 기동 | 브라우저에서 `/dashboard` 접속 | 4개 KPI 카드 모두 0이 아닌 실측값 표시, 콘솔 에러 없음 |

## TC-KPI: KPI 관리

| ID | 시나리오 | 절차 | 예상결과 |
|---|---|---|---|
| TC-KPI-01 | KPI 정의 카테고리 조회 | `GET /api/data-hub/analytics/definitions/categories/` | 200 |
| TC-KPI-02 | 최신 KPI 실적 조회 | `GET .../facts/latest/?limit=100` | 200, `results[]` |
| TC-KPI-03 | KPI 달성 현황(요약) 조회 | `GET .../facts/summary/?year=올해` | 200, `status_distribution` 키 존재 |
| TC-KPI-04 | KPI 재계산 요청 | `POST .../definitions/bulk_calculate/` | 정상 처리 또는 명시적 오류(크래시 아님) |
| TC-KPI-05 | KPI 관리 화면 렌더링(인증 실패 상황) | 미인증 상태로 `/kpiManagement` 접속 | 크래시 없이 "총 팩트 수 0" 등 안전한 폴백 표시 (회귀 테스트: 과거 `summary.status_distribution.good` 접근으로 크래시하던 결함 재발 방지) |
| TC-KPI-06 | KPI 카테고리 탭 전환 | 재무→원가→생산 탭 순차 클릭 | 매번 정상 로딩, 에러 없음 |

## TC-PROD: 생산 관리

| ID | 시나리오 | 절차 | 예상결과 |
|---|---|---|---|
| TC-PROD-01 | 생산 라인별 실적 집계 | 생산 대시보드 API 호출 | `lines[]`에 라인별 achievement 포함 |
| TC-PROD-02 | 날짜 미지정 시 최근접 데이터 대체 | 데이터 없는 날짜로 조회 | 가장 가까운 날짜 데이터로 200 응답(404 아님) |
| TC-PROD-03 | 생산 카드가 0으로 고정되지 않음 | 서로 다른 두 날짜 조회 후 비교 | `production_qty` 값이 다름 |

## TC-QUAL: 품질 관리

| ID | 시나리오 | 절차 | 예상결과 |
|---|---|---|---|
| TC-QUAL-01 | 검사유형별 합격률 조회 | 품질 대시보드 API 호출 | `pass_rate` 0~100 사이 |
| TC-QUAL-02 | 공정별(검사유형별) 수율 비교 | `yield_by_process[]` 확인 | incoming/in_process/final/outgoing 라벨 포함 |
| TC-QUAL-03 | LOT 추적 조회 | 온톨로지 AI 어시스턴트에 "LOT-XXX 추적" 질의 | 관련 자재/설비/작업자 응답 |
| TC-QUAL-04 | 검사 결과 0건 데이터 처리 | 데이터 없는 조건 | 404 또는 안전한 빈 상태(크래시 아님) |

## TC-SALES: 영업 관리

| ID | 시나리오 | 절차 | 예상결과 |
|---|---|---|---|
| TC-SALES-01 | 월별 매출 목표/실적 조회 | 매출 카드 확인 | 목표/실적 값이 억원 단위로 합리적 범위(수억~수십억) |
| TC-SALES-02 | 매출 상위 거래처 조회 | `top_customers[]` 확인 | 실제 거래처명(가공되지 않은 회사명) 포함 |

## TC-INV: 재고 관리

| ID | 시나리오 | 절차 | 예상결과 |
|---|---|---|---|
| TC-INV-01 | 재고 현황 조회 | 재고 카드 확인 | `total_stock_value>0` |
| TC-INV-02 | 부진재고 Top5 조회 | `slow_moving_details[]` 확인 | 5건 이하, `turnover_rate` 오름차순 |

## TC-ERP: ERP 연동/매핑

| ID | 시나리오 | 절차 | 예상결과 |
|---|---|---|---|
| TC-ERP-01 | YH 원격 연결 비활성 확인 | `is_connection_enabled('YH')` 호출 | `False` |
| TC-ERP-02 | LOCAL_BACKUP 폴백 연결 성공 | `test_connection('LOCAL_BACKUP')` | `status: success`, 실제 DB 버전/테이블 수 반환 |
| TC-ERP-03 | 시계열 자동 연장 | `extend_timeseries` 관리 명령 실행 | 최신 데이터가 오늘 이후까지 확장됨 |
| TC-ERP-04 | 장기 추정 값이 0으로 수렴하지 않음 | 실측 이후 20개월 이상 떨어진 기간 조회 | `revenue` 등 금액 필드가 과거 최솟값의 절반 이상 유지 |
| TC-ERP-05 | 연결설정 비밀번호 미노출 | `GET /api/erp-sync/connection-config/{id}/` | 응답에 `password` 키 없음 |
| TC-ERP-06 | 매핑 마스터 데이터 유효성 | `rebuild_mapping_master_data` 실행 후 매핑 목록 조회 | 모든 `target_model`이 실제 설치된 Django 앱을 가리킴 |

## TC-AUTH: 인증

| ID | 시나리오 | 절차 | 예상결과 |
|---|---|---|---|
| TC-AUTH-01 | 미인증 접근 차단 | 토큰 없이 보호된 API 호출 | 401/403 |
| TC-AUTH-02 | 정상 로그인 후 접근 | JWT 토큰으로 API 호출 | 200 |

## TC-UI: 공통 UI/라우팅

| ID | 시나리오 | 절차 | 예상결과 |
|---|---|---|---|
| TC-UI-01 | 반응형 사이드바 | 데스크톱/모바일 뷰포트 전환 | 1024px 기준 열림/닫힘 자동 전환 |
| TC-UI-02 | URL 라우팅 딥링크 | `/lotTrace` 직접 접속 후 새로고침 | 동일 화면 유지, 뒤로가기 시 이전 메뉴로 복원 |
