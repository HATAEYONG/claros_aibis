# 프로그램목록

**작성일**: 2026-07-08 | **버전**: 1.0

---

## 1. 백엔드 — Django 앱 목록 (33개)

| 앱 | 역할 | 비고 |
|---|---|---|
| `config` | 프로젝트 설정(settings/urls/celery/gunicorn) | INSTALLED_APPS 기준 |
| `accounting` | 관리회계(예산실적/부문손익/KPI성과/재무비율) | |
| `financial` | 재무제표/재무비율 | |
| `cost` | 원가(월별/제품별/드라이버/손익분기/구조) | |
| `sales` | 영업(월매출/제품/거래처등급/파이프라인/팀실적/Top거래처) | |
| `production` | 생산(라인/작업지시/일일실적) | |
| `quality` | 품질(검사) | |
| `purchase` | 구매/재고 | |
| `manufacturing` | 제조(사이클타임/OEE) | |
| `productivity` | 생산성(시간당생산/라인가동률/작업자/OEE요소/효율/일일요약) | |
| `esg` | ESG(점수/탄소배출/에너지/4M2E) | |
| `development` | R&D/혁신지표 | |
| `reports` | 경영보고(경영요약/부문비교/핵심지표/월간보고) | |
| `business_process` | O2C/P2P 비즈니스 프로세스 | 통합레이어(IntegratedMaterial 등) 포함 |
| `domain_agents` | 도메인 에이전트 분석(원가/품질/생산 등) | |
| `ontology` | 온톨로지(6M/4M2E), LOT 추적 | |
| `erp_sync` | ERP 연동/매핑/대시보드 데이터 서비스 | 이번 세션 핵심 작업 영역 |
| `ai` | AI 예측 엔진 | |
| `ai.copilot` | AI Copilot 채팅 인터페이스 | Phase 1 |
| `data_hub` | 데이터 통합 계층(KPI/Analytics, Integration Layer) | Phase 1 |
| `rag` | RAG(검색증강생성) | Phase 2 |
| `ml_pipeline` | ML 파이프라인 | Phase 3 |
| `forecasting` | 시계열 예측 | Phase 3 |
| `anomaly_detection` | 이상탐지 | Phase 3 |
| `integrations` | 외부 연동(범용) | Phase 4 |
| `visualization` | 데이터 시각화 | Phase 4 |
| `monitoring` | 시스템 모니터링 | Phase 5 |
| `security` | 보안/감사 | Phase 5 |
| `events` | 이벤트 기반 아키텍처 | AIBIS 확장 |
| `governance` | 거버넌스 레이어 | AIBIS 확장 |
| `control_tower` | 컨트롤 타워 | AIBIS 확장 |
| `axos_erp` | AXOS ERP V10.4 연동 | |
| `utils` | 공통 유틸(미들웨어, 시계열증강엔진, 관리명령) | |

## 2. 이번 세션에서 신규/변경된 주요 프로그램

### 2.1 신규 파일
| 경로 | 설명 |
|---|---|
| `backend/utils/timeseries_augmentation.py` | 시계열 자동 증강 엔진(모델 introspection + 추세/노이즈 투영) |
| `backend/utils/management/commands/extend_timeseries.py` | 시계열 연장 관리 명령(Celery Beat 연동) |
| `backend/utils/management/commands/import_real_yh_data.py` | 실제 YH 백업 ETL(생산/품질/매출/재고) |
| `backend/utils/management/commands/import_real_cost_data.py` | 실제 원가 데이터 매핑(분기 누적→월 손익) |
| `backend/erp_sync/management/commands/rebuild_mapping_master_data.py` | ERP 매핑 마스터 데이터 재구축 |
| `backend/erp_sync/migrations/0010_add_connection_config_model.py` | `ERPConnectionConfigModel` 마이그레이션 |
| `backend/erp_sync/data_hub/integration/{serializers,views,urls}.py` | 프로세스 통합 레이어 API |
| `frontend/src/components/business-process/ProcessChainView.tsx` | 프로세스 연관성 추적 화면 |
| `frontend/src/services/processChainService.ts` | 통합레이어 API 클라이언트 |

### 2.2 주요 변경 파일
| 경로 | 변경 요약 |
|---|---|
| `backend/erp_sync/services/dashboard_data_service.py` | 대시보드 5종을 YH 원격 연결 대신 로컬 ORM 직접 조회로 전면 재작성, `slow_moving_details`/`yield_by_process` 필드 추가 |
| `backend/erp_sync/services/erp_connection_service.py` | 중복 클래스 정의 버그 수정(신규 DB모델 기반 메서드가 죽은 코드였음) |
| `backend/erp_sync/serializers/connection_config_serializers.py` | `password` 필드 `write_only` 처리(보안) |
| `backend/erp_sync/views/mapping_views.py` | 항상 503을 반환하던 스텁을 실제 CRUD ViewSet으로 교체 |
| `backend/config/gunicorn.py` | `GUNICORN_WORKERS` 환경변수 지원(운영 메모리 제약 대응) |
| `frontend/src/App.tsx` | 사이드바 반응형 초기화 + URL 라우팅(주소창 동기화) 추가 |
| `frontend/src/components/dashboard/Dashboard.tsx` | 생산/품질/재고 API 응답을 실제로 소비하도록 전면 재작성(이전엔 응답을 버리고 있었음) |
| `frontend/src/services/api.ts` | `get()`에 쿼리 파라미터(`params`) 지원 추가 |
| `frontend/src/services/kpiService.ts` | 잘못된 `response.data` 접근 전량 제거(응답 계약 불일치 버그) |

## 3. 알려진 미해결/후속 항목

| 항목 | 내용 | 상태 |
|---|---|---|
| `masterDataService.ts` | `kpiService.ts`와 동일한 `response.data` 오접근 패턴 44곳 | 별도 백그라운드 작업으로 등록(미착수) |
| Celery worker 상시 구동 | 운영 서버 메모리 제약으로 Celery Beat 상시 구동은 미구성 — `extend_timeseries`는 수동/향후 cron 실행 필요 | 운영 시 확인 필요 |
