# 요구사항추적표 (Requirements Traceability Matrix)

**목적**: 요구사항정의서의 각 요구사항이 설계·구현·테스트 산출물과 정확히 매핑되는지 추적한다.

| 요구사항 ID | 설계 산출물 | 구현(Django 앱/모듈) | 구현(프론트엔드 컴포넌트) | 테스트 케이스 |
|---|---|---|---|---|
| REQ-101, REQ-102 | 화면설계서 §통합대시보드, API설계서 §대시보드 API | `erp_sync/services/dashboard_data_service.py` (`DashboardDataService`) | `dashboard/Dashboard.tsx` | TC-DASH-01~05 |
| REQ-103 | 화면설계서 §비즈니스프로세스, API설계서 §business_process | `business_process` | `dashboard/BusinessProcessSummary.tsx` | TC-DASH-06 |
| REQ-201~204 | 화면설계서 §KPI관리, API설계서 §KPI API, DB설계서 §data_hub | `data_hub`(analytics), `ai` | `kpi/KPIManagementPage.tsx`, `services/kpiService.ts` | TC-KPI-01~06 |
| REQ-301~303 | DB설계서 §production, API설계서 §생산 API | `production` | `dashboard/Dashboard.tsx`(생산 카드), `master/` | TC-PROD-01~03 |
| REQ-401~403 | DB설계서 §quality, API설계서 §품질 API | `quality` | `dashboard/Dashboard.tsx`(품질 카드), `ontology/`(LOT 추적) | TC-QUAL-01~04 |
| REQ-501~502 | DB설계서 §sales, API설계서 §영업 API | `sales` | `dashboard/Dashboard.tsx`(매출 카드) | TC-SALES-01~02 |
| REQ-601~602 | DB설계서 §purchase, API설계서 §재고 API | `purchase` | `dashboard/Dashboard.tsx`(재고 카드) | TC-INV-01~02 |
| REQ-701~703 | DB설계서 §cost/financial/accounting | `cost`, `financial`, `accounting` | `cost/`, `dashboard/FinancialManagement.tsx`, `ManagerialAccounting.tsx` | TC-COST-01~03 |
| REQ-801 | DB설계서 §esg | `esg` | `dashboard/ESG.tsx`, `FourM2EStrategy.tsx` | TC-ESG-01 |
| REQ-901~903 | 시스템아키텍처설계서 §AI서비스 | `ai`, `ontology`, `ai/copilot`, `rag` | `chat/AIAssistantChatV2.tsx`, `ontology/` | TC-AI-01~03 |
| REQ-1001~1004 | 인터페이스설계서(외부 ERP 연동) | `erp_sync` (models: `ERPConnectionConfigModel`, `ERPTableMapping` 등) | `erp/ERPSyncConfiguration.tsx` | TC-ERP-01~05 |
| REQ-1101~1102 | API설계서 §인증 | `rest_framework_simplejwt` (JWT), `config/urls.py` | `auth/` | TC-AUTH-01~02 |
| REQ-1201~1202 | 화면설계서 §반응형/라우팅 | — | `App.tsx`(사이드바 반응형, URL 라우팅) | TC-UI-01~02 |
| NFR-02, NFR-08 | 시스템아키텍처설계서 §시계열증강엔진 | `utils/timeseries_augmentation.py`, `utils/management/commands/extend_timeseries.py` | — | TC-ERP-03, TC-ERP-04 |
| NFR-03 | API설계서 §연결설정 | `erp_sync/serializers/connection_config_serializers.py` (password `write_only`) | — | TC-ERP-05 |
| NFR-06 | 배포절차서 §운영 안정성 | `config/gunicorn.py`(`GUNICORN_WORKERS` 환경변수) | — | 배포 후 운영 모니터링 |

## 커버리지 요약

| 상태 | 요구사항 수 |
|------|:---:|
| 설계·구현·테스트 모두 매핑됨 | 전체 |
| 구현만 있고 테스트 미작성 | 없음(04_테스트/테스트케이스.md에서 전수 커버) |
| 요구사항만 있고 미구현 | 없음 — 본 시스템은 이미 구축·운영 중 |
