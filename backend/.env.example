# ============================================================================
# Claros MIS-AI Dashboard Environment Variables
# ============================================================================

# ============================================================================
# Django Settings
# ============================================================================
DEBUG=True
SECRET_KEY=django-insecure-change-this-secret-key-in-production-min-50-chars
ALLOWED_HOSTS=localhost,127.0.0.1
SITE_URL=http://localhost:8000

# ============================================================================
# Database Configuration
# ============================================================================
# DB_TYPE Options:
#   - local  : 로컬 PostgreSQL (기본)
#   - prod   : 운영 YH PostgreSQL
#   - sqlite : SQLite (개발/테스트용)
DB_TYPE=local

# Local PostgreSQL Settings (DB_TYPE=local 일 때 사용)
DB_NAME=claros_mis
DB_USER=claros_user
DB_PASSWORD=claros_password
DB_HOST=localhost
DB_PORT=5432

# Production YH PostgreSQL Settings (DB_TYPE=prod 일 때 사용)
# DB_NAME=YH
# DB_USER=yh
# DB_PASSWORD=db!@yh#$1!
# DB_HOST=133.186.214.219
# DB_PORT=27455

# ============================================================================
# CORS Settings
# ============================================================================
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============================================================================
# Celery Configuration
# ============================================================================
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# ============================================================================
# ERP 연결 설정 (개선됨)
# ============================================================================

# YH ERP (PostgreSQL) 설정
YH_DB_HOST=133.186.214.219
YH_DB_PORT=27455
YH_DB_NAME=YH
YH_DB_USER=yh
YH_DB_PASSWORD=
YH_DB_ENABLED=False           # True: 연결 시도, False: 폴백 데이터만 사용
YH_DB_FALLBACK_TO_MOCK=True   # True: 연결 실패시 모의 데이터 사용
YH_DB_SUPPRESS_ERRORS=True    # True: 에러 로그 억제 (콘솔 노이즈 감소)
YH_DB_CONNECTION_TIMEOUT=10   # 연결 타임아웃 (초)
YH_DB_QUERY_TIMEOUT=30        # 쿼리 타임아웃 (초)

# FOM ERP (MS SQL) 설정
MSSQL_HOST=133.186.214.219
MSSQL_PORT=27455
MSSQL_USER=yh
MSSQL_PASSWORD=
MSSQL_DATABASE=YH
MSSQL_DRIVER=ODBC Driver 17 for SQL Server
MSSQL_ENCRYPT=no
MSSQL_TRUST_SERVER_CERTIFICATE=yes
MSSQL_CONNECTION_TIMEOUT=30
MSSQL_ENABLED=False            # True: 연결 시도, False: 폴백 데이터만 사용
MSSQL_FALLBACK_TO_MOCK=True   # True: 연결 실패시 모의 데이터 사용
MSSQL_SUPPRESS_ERRORS=True    # True: 에러 로그 억제

# AXOS ERP (Oracle) 설정
AXOS_DB_HOST=localhost
AXOS_DB_PORT=1521
AXOS_DB_NAME=AXOS
AXOS_DB_USER=axos
AXOS_DB_PASSWORD=
AXOS_ENABLED=False             # 기본 비활성화
AXOS_FALLBACK_TO_MOCK=True
AXOS_SUPPRESS_ERRORS=True

# ERP 연결 관리
ERP_CONNECTION_COOLDOWN=300    # 연결 실패 후 재시각 대기 시간 (초, 기본 5분)
ERP_MAX_RETRY_ATTEMPTS=3       # 최대 연결 재시도 횟수

# ============================================================================
# Sync Configuration
# ============================================================================
SYNC_BATCH_SIZE=1000
SYNC_RETRY_ATTEMPTS=3
SYNC_RETRY_DELAY=60
SYNC_TIMEOUT=300

# Table Sync Intervals (in seconds)
SYNC_SCHEDULE_REALTIME=900      # 15 minutes
SYNC_SCHEDULE_HOURLY=3600       # 1 hour
SYNC_SCHEDULE_DAILY=86400       # 1 day
SYNC_SCHEDULE_WEEKLY=604800     # 1 week
SYNC_SCHEDULE_MONTHLY=2592000   # 1 month

# ============================================================================
# KPI Configuration
# ============================================================================
KPI_CACHE_ENABLED=True
KPI_CACHE_TTL=300
KPI_CALCULATION_TIMEOUT=60

# ============================================================================
# AI Prediction Configuration
# ============================================================================
AI_PREDICTION_ENABLED=True
AI_MODEL_PATH=models/
AI_PREDICTION_CACHE_TTL=3600

# ============================================================================
# API 인증 설정 (개발 환경용)
# ============================================================================
API_AUTH_ENABLED=False         # True: 인증 필요, False: 인증 없이 접근 가능 (개발용)

# ============================================================================
# Celery Worker Configuration
# ============================================================================
CELERY_WORKER_CONCURRENCY=4
CELERY_WORKER_AUTOSCALE_MIN=2
CELERY_WORKER_AUTOSCALE_MAX=8

# ============================================================================
# Logging Configuration
# ============================================================================
LOG_LEVEL=INFO
LOG_FORMAT=text                # 'json' or 'text'
ELASTICSEARCH_URL=http://localhost:9200

# ============================================================================
# Prometheus Metrics
# ============================================================================
ENABLE_METRICS=True

# ============================================================================
# Adaptive Batch Configuration
# ============================================================================
ADAPTIVE_BATCH_ENABLED=True
BATCH_SIZE_MIN=100
BATCH_SIZE_MAX=5000
NETWORK_CHECK_INTERVAL=60

# ============================================================================
# Production Security Settings (DEBUG=False 일 때 적용)
# ============================================================================
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# ============================================================================
# Django 5.1 New Settings
# ============================================================================

# ============================================================================
# Redis Cache Configuration
# ============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
USE_HIREDIS=False

# ============================================================================
# Rate Limiting Configuration
# ============================================================================
RATELIMIT_ENABLE=True

# ============================================================================
# API Versioning Configuration
# ============================================================================
API_VERSION=v1

# ============================================================================
# WebSocket Settings
# ============================================================================
CHANNEL_LAYERS=redis
CHANNEL_HOST=localhost
CHANNEL_PORT=6379
