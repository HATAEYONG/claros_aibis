# 프론트엔드 구현 현황

## 개요

claros_aibis 프론트엔드 시스템의 구현 현황을 상세히 기술한다.

## 문서 개정 이력

| 버전 | 일자 | 작성자 | 수정 내용 |
|------|------|--------|----------|
| 1.0 | 2026-07-01 | AI Agent | 초기 작성 |

---

## 1. 구현 개요

### 1.1 프로젝트 구조

```
frontend/
├── public/                     # 정적 리소스
│   ├── index.html
│   └── favicon.ico
│
├── src/
│   ├── components/             # 컴포넌트
│   │   ├── common/            # 공통 컴포넌트
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Loading.tsx
│   │   │   └── ErrorState.tsx
│   │   │
│   │   ├── layout/            # 레이아웃 컴포넌트
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── MainLayout.tsx
│   │   │
│   │   ├── master/            # 마스터 데이터 관리
│   │   │   └── MasterDataManagement.tsx
│   │   │
│   │   ├── kpi/               # KPI 관리
│   │   │   ├── KPIManagementPage.tsx
│   │   │   ├── KPICard.tsx
│   │   │   └── KPIModal.tsx
│   │   │
│   │   └── dashboard/         # 대시보드
│   │       └── Dashboard.tsx
│   │
│   ├── services/              # API 서비스
│   │   ├── api.ts            # Axios 클라이언트
│   │   ├── masterDataService.ts
│   │   └── kpiService.ts
│   │
│   ├── hooks/                # 커스텀 훅
│   │   ├── useApi.ts
│   │   ├── useModal.ts
│   │   └── useToast.ts
│   │
│   ├── types/                # 타입 정의
│   │   ├── master.ts
│   │   ├── kpi.ts
│   │   └── common.ts
│   │
│   ├── utils/                # 유틸리티
│   │   ├── formatters.ts
│   │   ├── validators.ts
│   │   └── constants.ts
│   │
│   ├── styles/               # 스타일
│   │   └── globals.css
│   │
│   ├── App.tsx               # 메인 컴포넌트
│   └── main.tsx              # 진입점
│
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

---

## 2. 기술 스택

### 2.1 핵심 라이브러리

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| React | 19+ | UI 프레임워크 |
| TypeScript | 5+ | 타입 안전성 |
| Vite | 5+ | 빌드 도구 |
| Axios | 1.6+ | HTTP 클라이언트 |
| Recharts | 2+ | 데이터 시각화 |
| Lucide React | latest | 아이콘 |
| Tailwind CSS | 3+ | 스타일링 |

### 2.2 개발 도구

| 도구 | 용도 |
|------|------|
| ESLint | 코드 린트 |
| Prettier | 코드 포맷팅 |
| Vitest | 유닛 테스트 |
| Playwright | E2E 테스트 |

---

## 3. 서비스 계층 구현

### 3.1 API 클라이언트

**파일**: `src/services/api.ts`

```typescript
import axios from 'axios';

const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL || 'http://localhost:8000/api/',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 인터셉터
api.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('access_token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

api.interceptors.response.use(
  (response) => response,
  async (error) => {
    if (error.response?.status === 401) {
      // 토큰 갱신
      const refreshToken = localStorage.getItem('refresh_token');
      if (refreshToken) {
        // 갱신 로직
      }
    }
    return Promise.reject(error);
  }
);

export default api;
```

### 3.2 마스터 데이터 서비스

**파일**: `src/services/masterDataService.ts`

**구현된 API**:

| API | 메서드 | 경로 |
|-----|--------|------|
| accountAPI.getAccounts() | GET | /data-hub/master/accounts/ |
| accountAPI.getAccount() | GET | /data-hub/master/accounts/{id}/ |
| accountAPI.createAccount() | POST | /data-hub/master/accounts/ |
| accountAPI.updateAccount() | PUT | /data-hub/master/accounts/{id}/ |
| accountAPI.deleteAccount() | DELETE | /data-hub/master/accounts/{id}/ |
| accountAPI.exportAccounts() | POST | /data-hub/master/accounts/export/ |
| accountAPI.importAccounts() | POST | /data-hub/master/accounts/import/ |
| accountAPI.batchCreate() | POST | /data-hub/master/accounts/batch_create/ |
| accountAPI.batchUpdate() | POST | /data-hub/master/accounts/batch_update/ |
| accountAPI.batchDelete() | POST | /data-hub/master/accounts/batch_delete/ |

동일한 패턴으로 구현:
- warehouseAPI (창고)
- processAPI (공정)
- bankAPI (은행)

### 3.3 KPI 서비스

**파일**: `src/services/kpiService.ts`

**구현된 API**:

| API | 메서드 | 경로 |
|-----|--------|------|
| kpiAPI.getDefinitions() | GET | /data-hub/analytics/definitions/ |
| kpiAPI.getDefinition() | GET | /data-hub/analytics/definitions/{id}/ |
| kpiAPI.createDefinition() | POST | /data-hub/analytics/definitions/ |
| kpiAPI.updateDefinition() | PUT | /data-hub/analytics/definitions/{id}/ |
| kpiAPI.deleteDefinition() | DELETE | /data-hub/analytics/definitions/{id}/ |
| kpiAPI.getCategories() | GET | /data-hub/analytics/definitions/categories/ |
| kpiAPI.syncRegistry() | POST | /data-hub/analytics/definitions/sync_registry/ |
| kpiAPI.bulkCalculate() | POST | /data-hub/analytics/definitions/bulk_calculate/ |
| kpiAPI.getFacts() | GET | /data-hub/analytics/facts/ |
| kpiAPI.getLatestFacts() | GET | /data-hub/analytics/facts/latest/ |
| kpiAPI.getSummary() | GET | /data-hub/analytics/facts/summary/ |

---

## 4. 컴포넌트 구현

### 4.1 마스터 데이터 관리 페이지

**파일**: `src/components/master/MasterDataManagement.tsx`

**기능**:
- 4개 탭 (계정과목, 창고, 공정, 은행)
- 데이터 그리드 (검색, 필터, 정렬)
- CRUD 모달 (생성, 수정, 삭제)
- 일괄 작업 지원
- 내보내기/가져오기

```typescript
interface MasterDataManagementProps {
  // 없음 (라우팅 기반)
}

const MasterDataManagement: React.FC = () => {
  const [activeTab, setActiveTab] = useState<TabType>('account');
  const [data, setData] = useState<DataType[]>([]);
  const [loading, setLoading] = useState(false);
  const [modalOpen, setModalOpen] = useState(false);
  const [selectedItems, setSelectedItems] = useState<number[]>([]);

  // 데이터 로드
  const loadData = async () => { ... };

  // CRUD 핸들러
  const handleCreate = () => { ... };
  const handleEdit = (id: number) => { ... };
  const handleDelete = (id: number) => { ... };
  const handleBatchDelete = () => { ... };

  // 내보내기/가져오기
  const handleExport = (format: 'csv' | 'excel' | 'json') => { ... };
  const handleImport = (file: File) => { ... };

  return (
    <div className="master-data-management">
      <Tabs value={activeTab} onChange={setActiveTab}>
        <Tab value="account">계정과목</Tab>
        <Tab value="warehouse">창고</Tab>
        <Tab value="process">공정</Tab>
        <Tab value="bank">은행</Tab>
      </Tabs>

      <Filters onChange={loadData} />
      <Actions onExport={handleExport} onImport={handleImport} />

      <DataTable
        data={data}
        columns={columns[activeTab]}
        onSelect={setSelectedItems}
      />

      <Pagination />
    </div>
  );
};
```

### 4.2 KPI 관리 페이지

**파일**: `src/components/kpi/KPIManagementPage.tsx`

**기능**:
- 8개 카테고리 탭
- KPI 카드 그리드
- 실시간 상태 표시
- 대량 재계산
- 드릴다운

```typescript
const KPIManagementPage: React.FC = () => {
  const [activeCategory, setActiveCategory] = useState<string>('financial');
  const [kpis, setKpis] = useState<KPIDefinition[]>([]);
  const [facts, setFacts] = useState<KPIFact[]>([]);
  const [loading, setLoading] = useState(false);

  // KPI 로드
  const loadKPIs = async () => { ... };

  // 재계산
  const handleRecalculate = async (params: CalculationParams) => { ... };

  return (
    <div className="kpi-management">
      <CategoryTabs value={activeCategory} onChange={setActiveCategory} />

      <KPISummary facts={facts} />

      <KPIGrid>
        {kpis.map(kpi => (
          <KPICard
            key={kpi.kpi_id}
            kpi={kpi}
            fact={facts.find(f => f.kpi_id === kpi.kpi_id)}
            onClick={handleDrillDown}
          />
        ))}
      </KPIGrid>

      <RecalculateModal onConfirm={handleRecalculate} />
    </div>
  );
};
```

### 4.3 KPI 카드

**파일**: `src/components/kpi/KPICard.tsx`

**표시 정보**:
- KPI명
- 실적값 + 단위
- 목표값
- 상태 (양호/주의/위험/중립)
- 추세 (아이콘)
- 액션 버튼

```typescript
interface KPICardProps {
  kpi: KPIDefinition;
  fact?: KPIFact;
  showTrend?: boolean;
  onClick?: () => void;
}

const KPICard: React.FC<KPICardProps> = ({ kpi, fact, showTrend, onClick }) => {
  const status = getStatus(fact);
  const trend = getTrend(fact);

  return (
    <Card onClick={onClick} className={status}>
      <div className="kpi-card-header">
        <KPIIcon category={kpi.category} />
        <span>{kpi.kpi_name}</span>
      </div>

      <div className="kpi-card-value">
        <Value value={fact?.actual_value} unit={kpi.unit} />
        {showTrend && <Trend value={trend} />}
      </div>

      <div className="kpi-card-target">
        목표: {fact?.target_value} {kpi.unit}
      </div>

      <StatusBadge status={status} />

      <Actions>
        <Button icon={BarChart3} onClick={showChart} />
        <Button icon={FolderOpen} onClick={showDetails} />
        <Button icon={Settings} onClick={openSettings} />
      </Actions>
    </Card>
  );
};
```

---

## 5. 라우팅 구현

**파일**: `src/App.tsx`

```typescript
const App: React.FC = () => {
  const [activeMenu, setActiveMenu] = useState<MenuType>('home');

  const renderContent = () => {
    switch (activeMenu) {
      case 'home':
        return <Dashboard />;
      case 'masterData':
        return <MasterDataManagement />;
      case 'kpiManagement':
        return <KPIManagementPage />;
      case 'dashboard':
        return <Dashboard />;
      default:
        return <Dashboard />;
    }
  };

  return (
    <div className="app">
      <Header />
      <div className="app-body">
        <Sidebar activeMenu={activeMenu} onChange={setActiveMenu} />
        <Main>{renderContent()}</Main>
      </div>
    </div>
  );
};
```

---

## 6. 상태 관리

### 6.1 Auth Context

```typescript
interface AuthContextValue {
  user: User | null;
  token: string | null;
  login: (credentials: Credentials) => Promise<void>;
  logout: () => void;
  isAuthenticated: boolean;
}

export const AuthContext = createContext<AuthContextValue>(null);
```

### 6.2 App Context

```typescript
interface AppContextValue {
  loading: boolean;
  setLoading: (loading: boolean) => void;
  error: Error | null;
  setError: (error: Error | null) => void;
  toast: Toast | null;
  setToast: (toast: Toast | null) => void;
}

export const AppContext = createContext<AppContextValue>(null);
```

---

## 7. 스타일링

### 7.1 Tailwind CSS 설정

```javascript
// tailwind.config.js
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#E3F2FD',
          100: '#BBDEFB',
          500: '#2196F3',
          700: '#1976D2',
          900: '#0D47A1',
        },
        success: '#4CAF50',
        warning: '#FF9800',
        error: '#F44336',
      },
      fontFamily: {
        sans: ['Pretendard', 'sans-serif'],
      },
    },
  },
  plugins: [],
};
```

### 7.2 전역 스타일

```css
/* src/styles/globals.css */
@import '@fontsource/pretendard';

:root {
  --color-primary: #2196F3;
  --color-success: #4CAF50;
  --color-warning: #FF9800;
  --color-error: #F44336;
}

body {
  font-family: 'Pretendard', sans-serif;
  background-color: #F5F5F5;
}
```

---

## 8. 개발 서버 실행

```bash
# 프론트엔드 서버 시작
cd frontend
npm run dev

# 접속
http://localhost:3000
```

---

## 9. 구현 완료 항목

### 9.1 P0 항목 (완료)

| 컴포넌트 | 상태 | 비고 |
|----------|------|------|
| MasterDataManagement | ✅ 완료 | 4개 탭, CRUD, 일괄 작업 |
| KPICard | ✅ 완료 | 상태 표시, 액션 |
| KPIManagementPage | ✅ 완료 | 8개 카테고리, 재계산 |
| masterDataService | ✅ 완료 | 4개 마스터 API |
| kpiService | ✅ 완료 | KPI API 전체 |
| 공통 컴포넌트 | ✅ 완료 | Button, Modal, Card, Loading |

### 9.2 P1-P3 항목 (미구현)

| 컴포넌트 | 계획 |
|----------|------|
| ERP 연동 UI | 2026년 8월 |
| AI 분석 UI | 2026년 9월 |
| 경영 대시보드 | 2026년 10월 |
| 컨트롤 타워 | 2026년 10월 |

---

## 10. 빌드 및 배포

### 10.1 개발 빌드

```bash
npm run dev
```

### 10.2 프로덕션 빌드

```bash
npm run build
```

### 10.3 미리보기

```bash
npm run preview
```

---

## 11. 참고 문서

- [요구사항 정의](../01_요구사항/README.md)
- [UI/UX 설계](../02_설계/UIUX설계.md)
- [API 설계](../02_설계/API설계.md)
